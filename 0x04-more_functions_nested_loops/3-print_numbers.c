#include "main.h"

/**
 * print_numbers - The numbers speak for themselves
 *
 * Return: no return value
*/
void print_numbers(void)
{
	int i;

	for (i = 48; i < 58; i++)
		_putchar(i);
	_putchar(10);
}
