#include "main.h"

/**
 * mul - mul the two paramter the passed to it
 * @a : the first parameter
 * @b : the second parameter
 *
 * Return: 1 if is digit 0 otherwise 0
*/
int mul(int a, int b)
{
	return (a * b);
}
