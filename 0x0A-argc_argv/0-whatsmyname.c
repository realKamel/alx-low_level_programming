#include <stdio.h>

/**
 * main - Entery point
 * @argc: count of arguments
 * @argv: array of char pointer
 *
 * Return: 0 if success
*/
int main(__attribute__((unused))int argc, char **argv)
{
	printf("%s", argv[0]);
	return (0);
}
